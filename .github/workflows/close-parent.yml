name: Close parent when sub-issues complete

on:
  issues:
    types: [closed]

permissions: {}

jobs:
  run:
    permissions:
      issues: write
    uses: heindsight/agent-workflows/.github/workflows/close-parent.yml@998978dda0e4b9a091353356f319b2dcd0af2892  # v1
